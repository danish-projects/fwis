# Copy this file to .env.local and fill in your Supabase values.
# See docs/SUPABASE_SETUP.md for where to find each value.

# Postgres — pooler (port 6543) for the running app
DATABASE_URL="postgresql://postgres.xxxx:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Postgres — direct (port 5432) for migrations / Prisma CLI
DIRECT_URL="postgresql://postgres.xxxx:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# Supabase — Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://xxxx.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# Public site URL — MUST match your live URL before `npm run build:hosting`
# HTTP temp URL example: http://razarajwani-001-site17.dtempurl.com
# Production with SSL: https://yourdomain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Generate with: openssl rand -base64 32
PII_ENCRYPTION_KEY=""

# Optional — absent-parent email notifications
# RESEND_API_KEY=""
# EMAIL_FROM="FWIS <notifications@yourdomain.com>"

# Optional — Google Drive FWIS Docs PDFs (service account must have access)
# GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'
# Or split credentials:
# GOOGLE_DRIVE_SERVICE_ACCOUNT_EMAIL="lesson-plans@project.iam.gserviceaccount.com"
# GOOGLE_DRIVE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# Each academic year's folder ID (e.g. 2024-2025 under FWIS Docs) is set in Setup → Academic Years.
# Lesson plan PDFs are read from the "Lesson Plans" subfolder inside that year folder.
